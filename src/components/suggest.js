'use strict';

var React = require('react-native');
var {Component, View, Text} = React;

class Suggest extends Component{
  render(){
    return(
      <View><Text>Suggest's tab</Text></View>
    );
  }
}

module.exports = Suggest;
